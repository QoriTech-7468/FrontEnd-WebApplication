export class SignInResource {
    constructor({ id, name, surname, organizationId, token }) {
        this.id = id;
        this.name = name;
        this.surname = surname;
        this.organizationId = organizationId;
        this.token = token;
    }
}

