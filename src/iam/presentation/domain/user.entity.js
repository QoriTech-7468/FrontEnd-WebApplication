export class User {
    constructor({ id, name, surname, organizationId }) {
        this.id = id;
        this.name = name;
        this.surname = surname;
        this.organizationId = organizationId;
    }
}